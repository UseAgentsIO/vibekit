{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "interface:schedule configuration",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "tickMs": {
      "type": "number",
      "minimum": 0,
      "description": "Ticker interval in milliseconds. Default 15000. 0 disables the interval (manual tick only)."
    },
    "timezone": {
      "type": "string",
      "minLength": 1,
      "description": "Default IANA timezone for jobs that omit one. Default UTC."
    },
    "interfaceBinding": {
      "type": "string",
      "minLength": 1
    },
    "requireDelivery": {
      "type": "boolean",
      "description": "When true, jobs that name a deliveryInterface fail closed unless it is in knownInterfaces."
    },
    "knownInterfaces": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "scriptTimeoutMs": {
      "type": "number",
      "minimum": 1
    }
  }
}
