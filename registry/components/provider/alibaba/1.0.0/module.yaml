schemaVersion: 1

id: provider:alibaba
type: provider
name: alibaba
displayName: Alibaba Provider
version: 1.0.0
description: Declares Pi Alibaba DashScope compatible-mode provider configuration and a credential reference.

compatibility:
  vibekit: "^1.0.0"
  pi: ">=0.50.0"
  node: ">=20"

source:
  repository: "https://github.com/UseAgentsIO/vibekit"
  revision: "v1.0.0"

license: MIT

providesCapabilities:
  - provider.alibaba

requires:
  required: []
  optional: []
  recommended: []
  conflicts: []

requestsPermissions: []

secrets:
  - name: DASHSCOPE_API_KEY
    required: true
    source: environment

files:
  - source: payload/provider.yaml
    target: .vibekit/components/providers/alibaba.yaml
    ownership: exclusive

configuration:
  target: .vibekit/config/providers/alibaba.yaml
  schema: config.schema.json

healthCheck:
  type: pi-provider
  name: alibaba
