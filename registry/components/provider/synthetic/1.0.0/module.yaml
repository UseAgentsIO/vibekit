schemaVersion: 1

id: provider:synthetic
type: provider
name: synthetic
displayName: Synthetic
version: 1.0.0
description: Maps to Pi's synthetic provider.

compatibility:
  vibekit: "^1.0.0"
  pi: ">=0.50.0"
  node: ">=20"

source:
  repository: "https://github.com/UseAgentsIO/vibekit"
  revision: "v1.0.0"

license: MIT

providesCapabilities:
  - provider.synthetic

requires:
  required: []
  optional: []
  recommended: []
  conflicts: []

requestsPermissions: []

secrets:
  - name: SYNTHETIC_API_KEY
    required: true
    source: environment

files:
  - source: payload/provider.yaml
    target: .vibekit/components/providers/synthetic.yaml
    ownership: exclusive

configuration:
  target: .vibekit/config/providers/synthetic.yaml
  schema: config.schema.json

healthCheck:
  type: pi-provider
  name: synthetic
