schemaVersion: 1

id: provider:gmi
type: provider
name: gmi
displayName: GMI Provider
version: 1.0.0
description: Declares Pi GMI provider configuration and a credential reference.

compatibility:
  vibekit: "^1.0.0"
  pi: ">=0.50.0"
  node: ">=20"

source:
  repository: "https://github.com/UseAgentsIO/vibekit"
  revision: "v1.0.0"

license: MIT

providesCapabilities:
  - provider.gmi

requires:
  required: []
  optional: []
  recommended: []
  conflicts: []

requestsPermissions: []

secrets:
  - name: GMI_API_KEY
    required: true
    source: environment

files:
  - source: payload/provider.yaml
    target: .vibekit/components/providers/gmi.yaml
    ownership: exclusive

configuration:
  target: .vibekit/config/providers/gmi.yaml
  schema: config.schema.json

healthCheck:
  type: pi-provider
  name: gmi
