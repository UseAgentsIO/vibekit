schemaVersion: 1

id: provider:byteplus-plan
type: provider
name: byteplus-plan
displayName: BytePlus Plan
version: 1.0.0
description: Maps to Pi's byteplus-plan provider.

compatibility:
  vibekit: "^1.0.0"
  pi: ">=0.50.0"
  node: ">=20"

source:
  repository: "https://github.com/UseAgentsIO/vibekit"
  revision: "v1.0.0"

license: MIT

providesCapabilities:
  - provider.byteplus-plan

requires:
  required: []
  optional: []
  recommended: []
  conflicts: []

requestsPermissions: []

secrets:
  - name: BYTEPLUS_API_KEY
    required: true
    source: environment

files:
  - source: payload/provider.yaml
    target: .vibekit/components/providers/byteplus-plan.yaml
    ownership: exclusive

configuration:
  target: .vibekit/config/providers/byteplus-plan.yaml
  schema: config.schema.json

healthCheck:
  type: pi-provider
  name: byteplus-plan
