schemaVersion: 1

id: provider:featherless
type: provider
name: featherless
displayName: Featherless
version: 1.0.0
description: Maps to Pi's featherless provider.

compatibility:
  vibekit: "^1.0.0"
  pi: ">=0.50.0"
  node: ">=20"

source:
  repository: "https://github.com/UseAgentsIO/vibekit"
  revision: "v1.0.0"

license: MIT

providesCapabilities:
  - provider.featherless

requires:
  required: []
  optional: []
  recommended: []
  conflicts: []

requestsPermissions: []

secrets:
  - name: FEATHERLESS_API_KEY
    required: true
    source: environment

files:
  - source: payload/provider.yaml
    target: .vibekit/components/providers/featherless.yaml
    ownership: exclusive

configuration:
  target: .vibekit/config/providers/featherless.yaml
  schema: config.schema.json

healthCheck:
  type: pi-provider
  name: featherless
