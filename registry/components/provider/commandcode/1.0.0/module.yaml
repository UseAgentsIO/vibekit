schemaVersion: 1

id: provider:commandcode
type: provider
name: commandcode
displayName: CommandCode
version: 1.0.0
description: Maps to Pi's commandcode provider.

compatibility:
  vibekit: "^1.0.0"
  pi: ">=0.50.0"
  node: ">=20"

source:
  repository: "https://github.com/UseAgentsIO/vibekit"
  revision: "v1.0.0"

license: MIT

providesCapabilities:
  - provider.commandcode

requires:
  required: []
  optional: []
  recommended: []
  conflicts: []

requestsPermissions: []

secrets:
  - name: COMMANDCODE_API_KEY
    required: true
    source: environment

files:
  - source: payload/provider.yaml
    target: .vibekit/components/providers/commandcode.yaml
    ownership: exclusive

configuration:
  target: .vibekit/config/providers/commandcode.yaml
  schema: config.schema.json

healthCheck:
  type: pi-provider
  name: commandcode
