schemaVersion: 1

id: provider:venice
type: provider
name: venice
displayName: Venice
version: 1.0.0
description: Maps to Pi's venice provider.

compatibility:
  vibekit: "^1.0.0"
  pi: ">=0.50.0"
  node: ">=20"

source:
  repository: "https://github.com/UseAgentsIO/vibekit"
  revision: "v1.0.0"

license: MIT

providesCapabilities:
  - provider.venice

requires:
  required: []
  optional: []
  recommended: []
  conflicts: []

requestsPermissions: []

secrets:
  - name: VENICE_API_KEY
    required: true
    source: environment

files:
  - source: payload/provider.yaml
    target: .vibekit/components/providers/venice.yaml
    ownership: exclusive

configuration:
  target: .vibekit/config/providers/venice.yaml
  schema: config.schema.json

healthCheck:
  type: pi-provider
  name: venice
