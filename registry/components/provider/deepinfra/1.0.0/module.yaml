schemaVersion: 1

id: provider:deepinfra
type: provider
name: deepinfra
displayName: DeepInfra Provider
version: 1.0.0
description: Declares Pi DeepInfra provider configuration and a credential reference.

compatibility:
  vibekit: "^1.0.0"
  pi: ">=0.50.0"
  node: ">=20"

source:
  repository: "https://github.com/UseAgentsIO/vibekit"
  revision: "v1.0.0"

license: MIT

providesCapabilities:
  - provider.deepinfra

requires:
  required: []
  optional: []
  recommended: []
  conflicts: []

requestsPermissions: []

secrets:
  - name: DEEPINFRA_API_KEY
    required: true
    source: environment

files:
  - source: payload/provider.yaml
    target: .vibekit/components/providers/deepinfra.yaml
    ownership: exclusive

configuration:
  target: .vibekit/config/providers/deepinfra.yaml
  schema: config.schema.json

healthCheck:
  type: pi-provider
  name: deepinfra
