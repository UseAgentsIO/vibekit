schemaVersion: 1

id: provider:together
type: provider
name: together
displayName: Together Provider
version: 1.0.0
description: Declares Pi Together provider configuration and a credential reference.

compatibility:
  vibekit: "^1.0.0"
  pi: ">=0.50.0"
  node: ">=20"

source:
  repository: "https://github.com/UseAgentsIO/vibekit"
  revision: "v1.0.0"

license: MIT

providesCapabilities:
  - provider.together

requires:
  required: []
  optional: []
  recommended: []
  conflicts: []

requestsPermissions: []

secrets:
  - name: TOGETHER_API_KEY
    required: true
    source: environment

files:
  - source: payload/provider.yaml
    target: .vibekit/components/providers/together.yaml
    ownership: exclusive

configuration:
  target: .vibekit/config/providers/together.yaml
  schema: config.schema.json

healthCheck:
  type: pi-provider
  name: together
