schemaVersion: 1

id: provider:vllm
type: provider
name: vllm
displayName: vLLM
version: 1.0.0
description: Maps to Pi's vllm provider for a local OpenAI-compatible server.

compatibility:
  vibekit: "^1.0.0"
  pi: ">=0.50.0"
  node: ">=20"

source:
  repository: "https://github.com/UseAgentsIO/vibekit"
  revision: "v1.0.0"

license: MIT

providesCapabilities:
  - provider.vllm

requires:
  required: []
  optional: []
  recommended: []
  conflicts: []

requestsPermissions: []

secrets: []

files:
  - source: payload/provider.yaml
    target: .vibekit/components/providers/vllm.yaml
    ownership: exclusive

configuration:
  target: .vibekit/config/providers/vllm.yaml
  schema: config.schema.json

healthCheck:
  type: pi-provider
  name: vllm
