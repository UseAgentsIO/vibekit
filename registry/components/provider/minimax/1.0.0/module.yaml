schemaVersion: 1

id: provider:minimax
type: provider
name: minimax
displayName: MiniMax
version: 1.0.0
description: Maps to Pi's minimax provider.

compatibility:
  vibekit: "^1.0.0"
  pi: ">=0.50.0"
  node: ">=20"

source:
  repository: "https://github.com/UseAgentsIO/vibekit"
  revision: "v1.0.0"

license: MIT

providesCapabilities:
  - provider.minimax

requires:
  required: []
  optional: []
  recommended: []
  conflicts: []

requestsPermissions: []

secrets:
  - name: MINIMAX_API_KEY
    required: true
    source: environment

files:
  - source: payload/provider.yaml
    target: .vibekit/components/providers/minimax.yaml
    ownership: exclusive

configuration:
  target: .vibekit/config/providers/minimax.yaml
  schema: config.schema.json

healthCheck:
  type: pi-provider
  name: minimax
