schemaVersion: 1

id: provider:cerebras
type: provider
name: cerebras
displayName: Cerebras
version: 1.0.0
description: Maps to Pi's cerebras provider.

compatibility:
  vibekit: "^1.0.0"
  pi: ">=0.50.0"
  node: ">=20"

source:
  repository: "https://github.com/UseAgentsIO/vibekit"
  revision: "v1.0.0"

license: MIT

providesCapabilities:
  - provider.cerebras

requires:
  required: []
  optional: []
  recommended: []
  conflicts: []

requestsPermissions: []

secrets:
  - name: CEREBRAS_API_KEY
    required: true
    source: environment

files:
  - source: payload/provider.yaml
    target: .vibekit/components/providers/cerebras.yaml
    ownership: exclusive

configuration:
  target: .vibekit/config/providers/cerebras.yaml
  schema: config.schema.json

healthCheck:
  type: pi-provider
  name: cerebras
