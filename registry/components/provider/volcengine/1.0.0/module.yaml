schemaVersion: 1

id: provider:volcengine
type: provider
name: volcengine
displayName: Volcengine
version: 1.0.0
description: Maps to Pi's volcengine provider.

compatibility:
  vibekit: "^1.0.0"
  pi: ">=0.50.0"
  node: ">=20"

source:
  repository: "https://github.com/UseAgentsIO/vibekit"
  revision: "v1.0.0"

license: MIT

providesCapabilities:
  - provider.volcengine

requires:
  required: []
  optional: []
  recommended: []
  conflicts: []

requestsPermissions: []

secrets:
  - name: VOLCANO_ENGINE_API_KEY
    required: true
    source: environment

files:
  - source: payload/provider.yaml
    target: .vibekit/components/providers/volcengine.yaml
    ownership: exclusive

configuration:
  target: .vibekit/config/providers/volcengine.yaml
  schema: config.schema.json

healthCheck:
  type: pi-provider
  name: volcengine
