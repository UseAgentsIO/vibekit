schemaVersion: 1

id: provider:deepseek
type: provider
name: deepseek
displayName: DeepSeek
version: 1.0.0
description: Maps to Pi's deepseek provider.

compatibility:
  vibekit: "^1.0.0"
  pi: ">=0.50.0"
  node: ">=20"

source:
  repository: "https://github.com/UseAgentsIO/vibekit"
  revision: "v1.0.0"

license: MIT

providesCapabilities:
  - provider.deepseek

requires:
  required: []
  optional: []
  recommended: []
  conflicts: []

requestsPermissions: []

secrets:
  - name: DEEPSEEK_API_KEY
    required: true
    source: environment

files:
  - source: payload/provider.yaml
    target: .vibekit/components/providers/deepseek.yaml
    ownership: exclusive

configuration:
  target: .vibekit/config/providers/deepseek.yaml
  schema: config.schema.json

healthCheck:
  type: pi-provider
  name: deepseek
