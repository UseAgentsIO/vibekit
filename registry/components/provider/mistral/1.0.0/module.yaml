schemaVersion: 1

id: provider:mistral
type: provider
name: mistral
displayName: Mistral
version: 1.0.0
description: Maps to Pi's mistral provider.

compatibility:
  vibekit: "^1.0.0"
  pi: ">=0.50.0"
  node: ">=20"

source:
  repository: "https://github.com/UseAgentsIO/vibekit"
  revision: "v1.0.0"

license: MIT

providesCapabilities:
  - provider.mistral

requires:
  required: []
  optional: []
  recommended: []
  conflicts: []

requestsPermissions: []

secrets:
  - name: MISTRAL_API_KEY
    required: true
    source: environment

files:
  - source: payload/provider.yaml
    target: .vibekit/components/providers/mistral.yaml
    ownership: exclusive

configuration:
  target: .vibekit/config/providers/mistral.yaml
  schema: config.schema.json

healthCheck:
  type: pi-provider
  name: mistral
